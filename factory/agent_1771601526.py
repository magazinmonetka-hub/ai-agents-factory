Я могу помочь вам создать универсальный Dockerfile для ваших агентов!

Вот пример Dockerfile, который вы можете использовать:
```dockerfile
# Используем образ python:3.10-slim в качестве базового
FROM python:3.10-slim

# Устанавливаем необходимые пакеты
RUN pip install fastapi uvicorn

# Копируем содержимое папки factory в контейнер
COPY factory /app

# Устанавливаем рабочую директорию в /app
WORKDIR /app

# Запускаем приложение с помощью uvicorn
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```
Этот Dockerfile делает следующее:

1. Использует образ `python:3.10-slim` в качестве базового.
2. Устанавливает пакеты `fastapi` и `uvicorn` с помощью `pip`.
3. Копирует содержимое папки `factory` в контейнер в директорию `/app`.
4. Устанавливает рабочую директорию в `/app`.
5. Запускает приложение с помощью `uvicorn`, указывая хост и порт.

Чтобы сохранить этот Dockerfile в корне репозитория через узел GitHub, вы можете создать новый файл с именем `Dockerfile` в корне вашего репозитория и скопировать содержимое выше в него. Затем, вы можете закоммитить изменения и запушить их в ваш репозиторий на GitHub.

Например, если ваш репозиторий называется `my-repo`, вы можете создать новый файл `Dockerfile` в корне репозитория и запушить его в репозиторий следующим образом:
```bash
# Создаем новый файл Dockerfile в корне репозитория
touch Dockerfile

# Копируем содержимое выше в файл Dockerfile
# ...

# Зацикливаем изменения
git add Dockerfile
git commit -m "Добавлен Dockerfile для агентов"
git push origin main
```
После этого, ваш Dockerfile будет доступен в корне вашего репозитория на GitHub.